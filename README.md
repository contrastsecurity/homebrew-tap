# Homebrew Tap for Contrast

Add support for installing Contrast using Homebrew:

```shell
brew tap contrastsecurity/tap
```

[Contrast CLI](https://github.com/contrastsecurity/contrast)
```console
brew install contrast
```
